#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>
#include <ctype.h>

int scc(int code);    // Check return code of a function
void *scp(void *ptr); // Check if returned pointer of a function is NULL
